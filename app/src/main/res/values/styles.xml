<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="GradientButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/gradient_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18dp</item>
        <item name="android:minWidth">200dp</item>
        <item name="android:minHeight">20dp</item>
        <!-- Отключаем стандартный background -->
        <item name="android:backgroundTint">@null</item>
    </style>

    <style name="GradientButton_sw800dp" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/gradient_button</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">24dp</item>
        <item name="android:minWidth">400dp</item>
        <item name="android:minHeight">40dp</item>
        <item name="android:layout_marginBottom">40dp</item>
        <!-- Отключаем стандартный background -->
        <item name="android:backgroundTint">@null</item>
    </style>
</resources>